// Reuses the public events browser inside the resident dashboard shell so the
// join/leave/certificate logic (in EventDetail) isn't duplicated per role.
export { default } from '../public/EventsList';
